# ClassificadorEmails
Um sistema que utiliza o auxílio de uma API da OpenAI para classificar emails em produtivo e improdutivo e gerar uma resposta para cada mensagem enviada com base no seu conteúdo
